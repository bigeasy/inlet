sudo: false

language: node_js

node_js:
  - '6'
  - '8'
  - '10'

branches:
  only:
    - master
    - travis-ci

before_install:
  - npm install proof istanbul coveralls
  - for dir in inlet.*; do (cd $dir && npm install); done
