sudo: false

language: node_js

node_js:
  - '4'
  - '6'
  - '8'

branches:
  only:
    - master
    - travis-ci

before_install:
  - npm install proof istanbul coveralls
  - for dir in mingle.*; do (cd $dir && npm install); done
